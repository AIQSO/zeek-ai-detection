# Zeek node.cfg for a standalone sensor watching a dedicated capture
# interface. eth1 is the container interface attached to the capture
# bridge (vmbr2) and must be in promiscuous mode — the mirrored frames
# are addressed to other hosts' MACs.

[zeek]
type=standalone
host=localhost
interface=eth1
